"""Type aliases shared across the Tool Framework - a leaf module, like
every other subsystem's own types.py (kernel, runtime, agents)."""

from typing import Any, Mapping

Metadata = Mapping[str, Any]
